# Change Commits

- Create a separate Git commit after every completed code or configuration change.
- Before committing, review the diff and run relevant build or test commands.
- Stage only files changed for the current task. Do not include unrelated user changes.
- Use concise imperative commit subjects.
